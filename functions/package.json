{
  "name": "pluma-mietek-functions",
  "engines": { "node": "18" },
  "main": "src/index.js",
  "private": true,
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "dev": "firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.0.0",
    "node-fetch": "^3.3.0"
  }
}
